{
  "name": "authstar-clients-typescript",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "core",
    "fastify"
  ],
  "scripts": {
    "build": "npm run build -w core && npm run build -w fastify",
    "test": "npm run build -w core && npm run test --workspaces",
    "typecheck": "npm run build -w core && npm run typecheck --workspaces"
  },
  "allowScripts": {
    "esbuild@0.27.7": true
  }
}
